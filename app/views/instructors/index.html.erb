<table class="table table-striped table-dark">
  <thead class="thead">
    <tr>
    <form>
      <%# <th scope="col">Instructor Id</th> %>
      <th scope="col">First Name</th>
      <th scope="col">Last Name</th>
      <th scope="col">Position</th>
      <th scope="col">Quirk</th>
    </form>
    </tr>
    </thead>

    <tbody>
            <form>
            <tr> 
                <% @instructors.each do |single_instructor|%>
                <%# <td> <%= single_instructor.id%>
                <td class="nav-link" > <%= single_instructor.firstname%></td>
                <td><%= single_instructor.lastname %></td>
                <td> <%= single_instructor.postion%></td>
                <td><%= single_instructor.quirk%></td>
                <td><%= link_to "Edit", edit_instructor_path(single_instructor) %></td>
            </tr> 
            
            <% end %>
        </form>
    </tbody>
<table>
        